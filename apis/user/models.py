# from django.db import models
# from django.contrib.auth.models import AbstractUser
#
#
# class User(AbstractUser):
#     password_confirm = models.CharField(label='确认密码', min_length=6, max_length=20,
#                                              help_text='确认密码', write_only=True,
#                                              error_messages={'min_length': '最少要6个字符', 'max_length': '最大不超过20个字符'})
#     token = models.CharField(label='token值', read_only=True, help_text='token值')

